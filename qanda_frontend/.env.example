# PUBLIC_INTERFACE
# Frontend environment configuration for Company Q&A.
# Ensure this points to the backend API ROOT and ends with /api/
# In deployed environments behind a reverse proxy, you can leave this unset and the app will use relative /api/.
# For local dev with backend on 3001:
REACT_APP_BACKEND_API_URL=https://localhost:3001/api/
