# Frontend environment variables
# The orchestrator will populate .env; do not commit secrets.
# Backend API base URL (must include trailing slash and /api)
REACT_APP_BACKEND_API_URL=http://localhost:3001/api/
